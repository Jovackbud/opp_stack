{
  "name": "opptrack-functions",
  "version": "1.0.0",
  "engines": { "node": "20" },
  "main": "index.js",
  "scripts": {
    "lint": "node -c index.js && node -c scout.js && node -c analyst.js && node -c matcher.js && node -c notifier.js && node -c admin_config.js && node -c essay_assist.js && node -c whatsapp_digest.js"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.0.0",
    "@anthropic-ai/sdk": "^0.24.0",
    "rss-parser": "^3.13.0",
    "node-fetch": "^3.3.2",
    "cheerio": "^1.0.0-rc.12"
  }
}
